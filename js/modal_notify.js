/**
 * Creates the base of a modal 
 */
function md_base(){

}


/**
 * Generates a modal to be showed to notificate something at the site
 * use it instead of the alert function
 */
function md_notificate(message){

}
